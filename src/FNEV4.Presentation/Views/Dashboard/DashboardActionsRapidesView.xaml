<UserControl x:Class="FNEV4.Presentation.Views.Dashboard.DashboardActionsRapidesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Style pour les cartes d'actions -->
        <Style x:Key="ActionCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style pour les raccourcis -->
        <Style x:Key="RaccourciCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth1"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <materialDesign:PackIcon Kind="FlashAuto" 
                                           Width="32" Height="32" 
                                           Foreground="{DynamicResource PrimaryHueMidBrush}"
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="Actions Rapides" 
                             Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                             VerticalAlignment="Center"
                             Margin="12,0,0,0"/>
                </StackPanel>
                <TextBlock Text="Accès rapide aux fonctionnalités les plus utilisées"
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding ActualiserCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/>
                        <TextBlock Text="Actualiser" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Barre de statut -->
        <Border Grid.Row="1" 
                Background="{DynamicResource MaterialDesignChipBackground}"
                CornerRadius="4"
                Padding="16,8"
                Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Information" 
                                           Width="16" Height="16" 
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="{Binding StatusMessage}" 
                             VerticalAlignment="Center"
                             Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="{Binding StatistiqueClients}" 
                             VerticalAlignment="Center"
                             FontWeight="Medium"
                             Margin="0,0,16,0"/>
                    <TextBlock Text="•" 
                             VerticalAlignment="Center"
                             Margin="0,0,16,0"/>
                    <TextBlock Text="{Binding DerniereActivite}" 
                             VerticalAlignment="Center"
                             FontStyle="Italic"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenu principal -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Actions principales -->
            <materialDesign:Card Grid.Column="0" 
                               Style="{StaticResource DashboardCardStyle}"
                               Margin="0,0,12,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                        <materialDesign:PackIcon Kind="FlashAuto" 
                                               Width="24" Height="24" 
                                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                                               VerticalAlignment="Center"/>
                        <TextBlock Text="Actions Principales" 
                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                 VerticalAlignment="Center"
                                 Margin="12,0,0,0"/>
                    </StackPanel>

                    <!-- Grille d'actions -->
                    <ItemsControl ItemsSource="{Binding ActionsDisponibles}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3" Rows="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Style="{StaticResource ActionCardStyle}">
                                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                                            Command="{Binding Command}"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch"
                                            Padding="0"
                                            Height="120">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Border Background="{Binding Couleur}"
                                                  CornerRadius="24"
                                                  Width="48" Height="48"
                                                  Margin="0,0,0,12">
                                                <materialDesign:PackIcon Kind="{Binding Icone}"
                                                                       Width="24" Height="24"
                                                                       Foreground="White"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{Binding Titre}"
                                                     FontWeight="Medium"
                                                     HorizontalAlignment="Center"
                                                     Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding Description}"
                                                     FontSize="12"
                                                     Opacity="0.7"
                                                     HorizontalAlignment="Center"
                                                     TextWrapping="Wrap"
                                                     TextAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>

            <!-- Panneau latéral -->
            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                
                <!-- Raccourcis -->
                <materialDesign:Card Style="{StaticResource DashboardCardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <materialDesign:PackIcon Kind="BookmarkMultiple" 
                                                   Width="20" Height="20" 
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                   VerticalAlignment="Center"/>
                            <TextBlock Text="Raccourcis" 
                                     Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                     VerticalAlignment="Center"
                                     Margin="8,0,0,0"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Raccourcis}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card Style="{StaticResource RaccourciCardStyle}">
                                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                HorizontalContentAlignment="Left"
                                                Padding="0">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="{Binding Icone}"
                                                                       Width="16" Height="16"
                                                                       VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Nom}"
                                                         VerticalAlignment="Center"
                                                         Margin="8,0,0,0"/>
                                            </StackPanel>
                                        </Button>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Statut système rapide -->
                <materialDesign:Card Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <materialDesign:PackIcon Kind="MonitorDashboard" 
                                                   Width="20" Height="20" 
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                   VerticalAlignment="Center"/>
                            <TextBlock Text="Statut Rapide" 
                                     Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                     VerticalAlignment="Center"
                                     Margin="8,0,0,0"/>
                        </StackPanel>

                        <StackPanel>
                            <Border Background="{DynamicResource MaterialDesignChipBackground}"
                                  CornerRadius="12"
                                  Padding="12,6"
                                  Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="CheckCircle" 
                                                           Width="16" Height="16" 
                                                           Foreground="Green"
                                                           VerticalAlignment="Center"/>
                                    <TextBlock Text="Base de données : OK" 
                                             VerticalAlignment="Center"
                                             Margin="8,0,0,0"
                                             FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource MaterialDesignChipBackground}"
                                  CornerRadius="12"
                                  Padding="12,6"
                                  Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="CheckCircle" 
                                                           Width="16" Height="16" 
                                                           Foreground="Green"
                                                           VerticalAlignment="Center"/>
                                    <TextBlock Text="Configuration : OK" 
                                             VerticalAlignment="Center"
                                             Margin="8,0,0,0"
                                             FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource MaterialDesignChipBackground}"
                                  CornerRadius="12"
                                  Padding="12,6">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Information" 
                                                           Width="16" Height="16" 
                                                           Foreground="Orange"
                                                           VerticalAlignment="Center"/>
                                    <TextBlock Text="API FNE : En attente" 
                                             VerticalAlignment="Center"
                                             Margin="8,0,0,0"
                                             FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </Grid>

        <!-- Indicateur de chargement -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <materialDesign:Card HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Padding="32">
                <StackPanel HorizontalAlignment="Center">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                               Value="0" IsIndeterminate="True"
                               Width="48" Height="48"
                               Margin="0,0,0,16"/>
                    <TextBlock Text="Traitement en cours..."
                             HorizontalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>
        </Border>
    </Grid>
</UserControl>
