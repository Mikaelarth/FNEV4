<UserControl x:Class="FNEV4.Presentation.Views.GestionFactures.FacturesFneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:FNEV4.Presentation.ViewModels.GestionFactures"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:FacturesFneViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- En-tête avec titre et statistiques -->
        <materialDesign:Card Grid.Row="0" Margin="16,16,16,8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Titre et description -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Factures FNE" 
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               Margin="0,0,0,4" />
                    <TextBlock Text="Liste des factures FNE importées et gérées" 
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Opacity="0.6" />
                </StackPanel>

                <!-- Statistiques rapides -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <materialDesign:Card materialDesign:ShadowAssist.ShadowDepth="Depth1" Margin="0,0,8,0" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="{Binding TotalFactures}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Total" FontSize="12" Opacity="0.6" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card materialDesign:ShadowAssist.ShadowDepth="Depth1" Margin="0,0,8,0" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="{Binding FacturesDraft}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Draft" FontSize="12" Opacity="0.6" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card materialDesign:ShadowAssist.ShadowDepth="Depth1" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="{Binding FacturesCertifiees}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Certifiées" FontSize="12" Opacity="0.6" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Barre d'outils avec recherche et filtres -->
        <materialDesign:Card Grid.Row="1" Margin="16,0,16,8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Recherche -->
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Rechercher par numéro, client..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,16,0" />

                <!-- Filtre par statut -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding StatusOptions}"
                          SelectedItem="{Binding SelectedStatus}"
                          materialDesign:HintAssist.Hint="Filtrer par statut"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="0,0,16,0" />

                <!-- Boutons d'action -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="{materialDesign:PackIcon Kind=Refresh}"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Actualiser"
                            Margin="0,0,8,0" />
                    
                    <Button Content="{materialDesign:PackIcon Kind=FilterVariant}"
                            Command="{Binding ApplyFiltersCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Appliquer les filtres"
                            Margin="0,0,8,0" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Liste des factures -->
        <materialDesign:Card Grid.Row="2" Margin="16,0,16,8">
            <Grid>
                <DataGrid ItemsSource="{Binding Factures}"
                          SelectedItem="{Binding SelectedFacture}"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          AlternatingRowBackground="#F8F9FA"
                          RowBackground="White"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <!-- Numéro de facture -->
                        <DataGridTextColumn Header="Numéro"
                                            Binding="{Binding InvoiceNumber}"
                                            Width="120" />

                        <!-- Date facture -->
                        <DataGridTextColumn Header="Date"
                                            Binding="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}"
                                            Width="100" />

                        <!-- Client -->
                        <DataGridTextColumn Header="Client"
                                            Binding="{Binding ClientDisplayName}"
                                            Width="200" />

                        <!-- Code client -->
                        <DataGridTextColumn Header="Code Client"
                                            Binding="{Binding ClientCode}"
                                            Width="120" />

                        <!-- Statut -->
                        <DataGridTemplateColumn Header="Statut" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="8,4" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="draft">
                                                        <Setter Property="Background" Value="LightGray" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="validated">
                                                        <Setter Property="Background" Value="LightBlue" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="certified">
                                                        <Setter Property="Background" Value="LightGreen" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="error">
                                                        <Setter Property="Background" Value="LightCoral" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" 
                                                   FontSize="11" 
                                                   FontWeight="Medium"
                                                   Foreground="White" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Montant TTC -->
                        <DataGridTextColumn Header="Montant TTC"
                                            Binding="{Binding TotalAmountTTC, StringFormat={}{0:N2}}"
                                            Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Point de vente -->
                        <DataGridTextColumn Header="Point de vente"
                                            Binding="{Binding PointOfSale}"
                                            Width="100" />

                        <!-- Actions -->
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="{materialDesign:PackIcon Kind=Eye}"
                                                Command="{Binding DataContext.ViewFactureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Voir les détails"
                                                Margin="2" />

                                        <Button Content="{materialDesign:PackIcon Kind=Certificate}"
                                                Command="{Binding DataContext.CertifyFactureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Certifier"
                                                Margin="2">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="certified">
                                                            <Setter Property="IsEnabled" Value="False" />
                                                            <Setter Property="Opacity" Value="0.5" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Content="{materialDesign:PackIcon Kind=Delete}"
                                                Command="{Binding DataContext.DeleteFactureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Supprimer"
                                                Foreground="Red"
                                                Margin="2">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="certified">
                                                            <Setter Property="IsEnabled" Value="False" />
                                                            <Setter Property="Opacity" Value="0.5" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Overlay de chargement -->
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                      Background="White" Opacity="0.8">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True"
                                     Width="50" Height="50" />
                        <TextBlock Text="Chargement des factures..."
                                   Margin="0,16,0,0"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Grid>
        </materialDesign:Card>

        <!-- Barre de statut -->
        <materialDesign:Card Grid.Row="3" Margin="16,0,16,16" Padding="8">
            <TextBlock Text="{Binding StatusMessage}" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Opacity="0.6" />
        </materialDesign:Card>
    </Grid>
</UserControl>