<Window x:Class="FNEV4.Presentation.Views.GestionFactures.FactureDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        Title="Détails de la Facture" 
        Height="700" Width="1200"
        MinHeight="500"
        MinWidth="900"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <!-- Style pour les en-têtes de section -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Margin" Value="0,12,0,8"/>
        </Style>

        <!-- Style pour les valeurs -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="8,2"/>
        </Style>

        <!-- Style pour les badges de statut -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="8,2"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête avec icône et titre -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <materialDesign:PackIcon Grid.Column="0"
                                     Kind="FileDocumentOutline"
                                     Width="32"
                                     Height="32"
                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,12,0"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Facture.InvoiceNumber, StringFormat='🧾 Facture N° {0}'}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBlock Text="{Binding Facture.InvoiceDate, StringFormat='Émise le {0:dd/MM/yyyy}'}"
                           FontSize="14"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>

            <!-- Badge de statut -->
            <Border Grid.Column="2">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                        <Setter Property="Background" Value="LightGray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Facture.Status}" Value="Certified">
                                <Setter Property="Background" Value="#4CAF50"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Facture.Status}" Value="Error">
                                <Setter Property="Background" Value="#F44336"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Facture.Status}" Value="En cours certification">
                                <Setter Property="Background" Value="#2196F3"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding Facture.Status}"
                           FontWeight="SemiBold"
                           Foreground="White"/>
            </Border>
        </Grid>

        <!-- Informations de la facture -->
        <materialDesign:Card Grid.Row="1" 
                             Margin="0,0,0,16"
                             Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Colonne 1: Informations client -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="📋 INFORMATIONS CLIENT" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Code:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.Client.ClientCode}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nom:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Facture.Client.Name}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Template:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <Border Grid.Row="2" Grid.Column="1" 
                                Background="{Binding TemplateBackground}"
                                CornerRadius="8"
                                Padding="6,2"
                                HorizontalAlignment="Left"
                                Margin="8,2">
                            <TextBlock Text="{Binding Facture.Client.DefaultTemplate}" 
                                       FontWeight="SemiBold"
                                       Foreground="White"/>
                        </Border>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="NCC:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Facture.Client.ClientNcc}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Point de vente:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Facture.PointOfSale}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Colonne 2: Informations facture -->
                <StackPanel Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="📄 INFORMATIONS FACTURE" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="N° Facture:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.InvoiceNumber}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Date:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Facture.InvoiceDate, StringFormat=dd/MM/yyyy}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Nb Articles:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Facture.Items.Count}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Colonne 3: Montants -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="💰 MONTANTS" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Montant HT:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.TotalAmountHT, StringFormat=N2}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="TVA:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TvaAmount, StringFormat=N2}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Montant TTC:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Facture.TotalAmountTTC, StringFormat=N2}" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- DataGrid des articles -->
        <materialDesign:Card Grid.Row="2" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- En-tête des articles -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Padding="16,12">
                    <TextBlock Text="🛍️ DÉTAIL DES ARTICLES"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Facture.Items}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          AlternatingRowBackground="{DynamicResource MaterialDesignDivider}"
                          materialDesign:DataGridAssist.CellPadding="8,4"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8,8">

                    <DataGrid.Columns>
                        <!-- Code Produit -->
                        <DataGridTextColumn Header="Code Article"
                                            Binding="{Binding ProductCode}"
                                            Width="120"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Désignation -->
                        <DataGridTextColumn Header="Désignation"
                                            Binding="{Binding Description}"
                                            Width="*"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Quantité -->
                        <DataGridTextColumn Header="Qté"
                                            Binding="{Binding Quantity, StringFormat=N2}"
                                            Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Emballage -->
                        <DataGridTextColumn Header="Emballage"
                                            Binding="{Binding MeasurementUnit}"
                                            Width="100"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Prix Unitaire -->
                        <DataGridTextColumn Header="Prix Unit."
                                            Binding="{Binding UnitPrice, StringFormat=N2}"
                                            Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Type TVA -->
                        <DataGridTemplateColumn Header="Type TVA" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                        <!-- Code TVA -->
                                        <TextBlock HorizontalAlignment="Center"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Margin="0,2">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="">
                                                            <Setter Property="Text" Value="NON DÉFINI"/>
                                                            <Setter Property="Foreground" Value="#FF5722"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="NON DÉFINI"/>
                                                            <Setter Property="Foreground" Value="#FF5722"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                    <Setter Property="Text" Value="{Binding VatCode}"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <!-- Description TVA -->
                                        <TextBlock FontSize="10" 
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,2">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="TVA">
                                                            <Setter Property="Text" Value="TVA normal de 18%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="TVAB">
                                                            <Setter Property="Text" Value="TVA réduit de 9%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="TVAC">
                                                            <Setter Property="Text" Value="TVA exec conv de 0%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="TVAD">
                                                            <Setter Property="Text" Value="TVA exec leg de 0% pour TEE et RME"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="">
                                                            <Setter Property="Text" Value="Code TVA manquant"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding VatCode}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="Code TVA manquant"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                    <Setter Property="Text" Value="Inconnu"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Montant HT -->
                        <DataGridTextColumn Header="Total HT"
                                            Binding="{Binding LineAmountHT, StringFormat=N2}"
                                            Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Boutons d'action -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            
            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding PrintCommand}"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Printer" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Imprimer"/>
                </StackPanel>
            </Button>
            
            <Button Content="❌ FERMER"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Padding="16,8"
                    Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>