<UserControl x:Class="FNEV4.Presentation.Views.ImportTraitement.Sage100ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:FNEV4.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
        <converters:BoolToIconConverter x:Key="BoolToIconConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToStateConverter x:Key="BoolToStateConverter"/>
        <converters:ProcessingTextConverter x:Key="ProcessingTextConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanAndConverter x:Key="BooleanAndConverter"/>
        <converters:ProduitsToTooltipConverter x:Key="ProduitsToTooltipConverter"/>
        
        <!-- Nouveaux convertisseurs pour améliorer la DataGrid -->
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>
        <converters:BoolToLightColorConverter x:Key="BoolToLightColorConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignHeadline5TextBlock}">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <Style x:Key="StepCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <materialDesign:PackIcon Grid.Column="0" 
                                   Kind="AlertCircleOutline" 
                                   Width="40" Height="40"
                                   VerticalAlignment="Center"
                                   Foreground="#FF5722"/>
            
            <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Import Exceptionnel Sage 100 v15" 
                         Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Import spécialisé pour factures Sage 100 v15 avec structure 1 feuille = 1 facture" 
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
            
            <Button Grid.Column="2" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding GoBackCommand}"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Retour"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Zone principale -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Section: Dossiers configurés et import automatique -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}" 
                                   Background="{DynamicResource MaterialDesignPaper}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Kind="FolderCog" 
                                                   Grid.Column="0"
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,12,0"
                                                   Width="24" Height="24"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Dossiers Configurés - Import Automatique" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="Traitement automatisé des fichiers depuis le dossier d'import configuré" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         Margin="0,2,0,0"/>
                            </StackPanel>
                            
                            <Button Grid.Column="2" 
                                   Style="{StaticResource MaterialDesignRaisedButton}"
                                   Background="{DynamicResource PrimaryHueMidBrush}"
                                   Command="{Binding ScanAndPreviewCommand}"
                                   Margin="0,0,8,0"
                                   Width="110">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="MagnifyPlusOutline" Width="18" Height="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="Scanner"/>
                                </StackPanel>
                                <Button.ToolTip>
                                    <ToolTip Content="Scanner le dossier d'import et afficher automatiquement l'aperçu des factures trouvées"/>
                                </Button.ToolTip>
                            </Button>
                            
                            <Button Grid.Column="3" 
                                   Style="{StaticResource MaterialDesignIconButton}"
                                   Command="{Binding OpenConfiguredFoldersCommand}"
                                   Width="40" Height="40">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="20" Height="20"/>
                                <Button.ToolTip>
                                    <ToolTip Content="Ouvrir le dossier d'import dans l'explorateur"/>
                                </Button.ToolTip>
                            </Button>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Dossier Import -->
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="FolderDownload" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#4CAF50"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Import" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ImportFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ImportFolderPath}"/>
                            </StackPanel>
                            
                            <!-- Dossier Export -->
                            <StackPanel Grid.Column="1" Margin="0,0,16,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="FolderUpload" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#2196F3"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Export" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ExportFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ExportFolderPath}"/>
                            </StackPanel>
                            
                            <!-- Dossier Archive -->
                            <StackPanel Grid.Column="2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="Archive" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#FF9800"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Archive" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ArchiveFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ArchiveFolderPath}"/>
                            </StackPanel>
                        </Grid>
                        
                        <CheckBox Content="Archiver automatiquement les fichiers après import" 
                                  IsChecked="{Binding AutoArchiveEnabled}"
                                  Margin="0,16,0,0"
                                  Style="{StaticResource MaterialDesignCheckBox}"/>
                    </StackPanel>
                </materialDesign:Card>

                <Separator Margin="0,8,0,16" Style="{StaticResource MaterialDesignSeparator}"/>
                
                <!-- Étape 1: Sélection du fichier -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" 
                               Background="#E3F2FD" 
                               CornerRadius="20" 
                               Width="40" Height="40"
                               Margin="0,0,16,0">
                            <TextBlock Text="1" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"
                                     FontWeight="Bold" 
                                     FontSize="16"
                                     Foreground="#1976D2"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Import Manuel - Sélection de fichier" 
                                     Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                     FontWeight="Medium"/>
                            <TextBlock Text="{Binding SelectedFilePath, TargetNullValue='Aucun fichier sélectionné - Alternative à l\'import automatique'}" 
                                     Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                                     Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <Button Grid.Column="2" 
                               Content="Parcourir"
                               Style="{StaticResource MaterialDesignRaisedButton}"
                               Command="{Binding SelectFileCommand}"
                               Width="120"/>
                    </Grid>
                </materialDesign:Card>

                <!-- Étape 2: Validation et aperçu -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding HasSelectedFile, Converter={StaticResource BoolToVisConverter}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- En-tête étape 2 -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                   Background="#E8F5E8" 
                                   CornerRadius="20" 
                                   Width="40" Height="40"
                                   Margin="0,0,16,0">
                                <TextBlock Text="2" 
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center"
                                         FontWeight="Bold" 
                                         FontSize="16"
                                         Foreground="#388E3C"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Validation et aperçu du fichier" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="Vérification de la structure Sage 100 v15 et prévisualisation des factures" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            </StackPanel>
                            
                            <Button Grid.Column="2" 
                                   Style="{StaticResource MaterialDesignRaisedButton}"
                                   Command="{Binding ValidateFileCommand}"
                                   Width="120">
                                <Button.IsEnabled>
                                    <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                                        <Binding Path="HasSelectedFile"/>
                                        <Binding Path="IsProcessing" Converter="{StaticResource InverseBooleanConverter}"/>
                                    </MultiBinding>
                                </Button.IsEnabled>
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="{Binding ValidationIcon, FallbackValue=CheckCircle}"
                                                               Width="16" Height="16"
                                                               Margin="0,0,8,0"
                                                               Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard RepeatBehavior="Forever">
                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle"
                                                                                       From="0" To="360" Duration="0:0:1"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <Setter Property="Kind" Value="Loading"/>
                                                            <Setter Property="RenderTransform">
                                                                <Setter.Value>
                                                                    <RotateTransform/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding IsProcessing, Converter={StaticResource ProcessingTextConverter}, FallbackValue=Valider}"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </Grid>
                        
                        <!-- Statistiques filtrables style Preview Window -->
                        <materialDesign:Card Padding="16" Margin="0,16,0,16" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Statistiques compactes avec filtres -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    
                                    <!-- Bouton filtre: Toutes -->
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                            Command="{Binding ShowAllFacturesCommand}"
                                            Height="32" Margin="0,0,8,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="FileDocument" Width="16" Height="16" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding TotalFiles, StringFormat='{}{0} trouvées'}" FontSize="12" FontWeight="Medium"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <!-- Bouton filtre: Valides -->
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                            Command="{Binding ShowValidFacturesCommand}"
                                            Height="32" Margin="0,0,8,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="CheckCircle" Width="16" Height="16" Margin="0,0,6,0" Foreground="#4CAF50"/>
                                            <TextBlock Text="{Binding ValidFiles, StringFormat='{}{0} valides'}" FontSize="12" FontWeight="Medium"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <!-- Bouton filtre: Doublons -->
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                            Command="{Binding ShowDoublonsFacturesCommand}"
                                            Height="32" Margin="0,0,8,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="ContentDuplicate" Width="16" Height="16" Margin="0,0,6,0" Foreground="#FF9800"/>
                                            <TextBlock Text="{Binding DoublonsFiles, StringFormat='{}{0} doublons'}" FontSize="12" FontWeight="Medium"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <!-- Bouton filtre: Erreurs -->
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                            Command="{Binding ShowErrorFacturesCommand}"
                                            Height="32">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="AlertCircle" Width="16" Height="16" Margin="0,0,6,0" Foreground="#F44336"/>
                                            <TextBlock Text="{Binding InvalidFiles, StringFormat='{}{0} erreurs'}" FontSize="12" FontWeight="Medium"/>
                                        </StackPanel>
                                    </Button>
                                    
                                </StackPanel>
                                
                                <!-- Recherche compacte -->
                                <Grid Grid.Column="1" Width="300">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBox Grid.Column="0" 
                                             materialDesign:HintAssist.Hint="Recherche..."
                                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="13" 
                                             Height="36" 
                                             VerticalAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             Padding="0,8,0,0" />
                                    
                                    <Button Grid.Column="1" 
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding ClearSearchCommand}"
                                            Width="36" Height="36" 
                                            Margin="4,0,0,0"
                                            VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>
                        
                        <!-- Aperçu des factures -->
                        <materialDesign:Card Grid.Row="4" 
                                           Padding="0" 
                                           Margin="0,0,0,24"
                                           materialDesign:ShadowAssist.ShadowDepth="Depth2"
                                           Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVisConverter}}">
                            <DataGrid Name="FacturesDataGrid"
                                      ItemsSource="{Binding PreviewFacturesView}"
                                      AutoGenerateColumns="False"
                                      CanUserSortColumns="True"
                                      CanUserReorderColumns="False"
                                      CanUserResizeColumns="True"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      materialDesign:DataGridAssist.CellPadding="8,6"
                                      materialDesign:DataGridAssist.ColumnHeaderPadding="8,8"
                                      FontSize="12"
                                      RowHeight="40"
                                      IsReadOnly="True"
                                      EnableRowVirtualization="True"
                                      EnableColumnVirtualization="True"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      VirtualizingPanel.VirtualizationMode="Recycling"
                                      VirtualizingPanel.IsContainerVirtualizable="True"
                                      ScrollViewer.CanContentScroll="True"
                                      ClipToBounds="True"
                                      UseLayoutRounding="True"
                                      SnapsToDevicePixels="True">

                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="N° Facture" Binding="{Binding NumeroFacture}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Date" Binding="{Binding DateFacture, StringFormat=dd/MM/yyyy}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Code Client" Binding="{Binding CodeClient}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Client" Binding="{Binding NomClient}" Width="200">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    
                                    <!-- Colonne Template avec style sémantique -->
                                    <DataGridTemplateColumn Header="Template" Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent" 
                                                        Padding="6,4"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center">
                                                    <TextBlock Text="{Binding Template}" 
                                                              FontWeight="Medium"
                                                              FontSize="11"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Template}" Value="B2B">
                                                                        <Setter Property="Foreground" Value="#2196F3"/>
                                                                        <Setter Property="Background" Value="#E3F2FD"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Template}" Value="B2C">
                                                                        <Setter Property="Foreground" Value="#FF9800"/>
                                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Template}" Value="N/A">
                                                                        <Setter Property="Foreground" Value="#757575"/>
                                                                        <Setter Property="Background" Value="#F5F5F5"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    
                                    <DataGridTextColumn Header="Point de Vente" Binding="{Binding PointDeVente}" Width="130">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Moyen Paiement" Binding="{Binding MoyenPaiement}" Width="130">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Montant HT" Binding="{Binding MontantHT, StringFormat=N2}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Montant TTC" Binding="{Binding MontantTTC, StringFormat=N2}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Padding" Value="4,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    
                                    <!-- Colonne Statut avec couleurs sémantiques -->
                                    <DataGridTemplateColumn Header="Statut" Width="90">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent" 
                                                        Padding="8,4"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Statut}" 
                                                              FontWeight="Medium"
                                                              FontSize="12"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Statut}" Value="Valide">
                                                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Statut}" Value="Erreur">
                                                                        <Setter Property="Foreground" Value="#F44336"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Statut}" Value="Doublon">
                                                                        <Setter Property="Foreground" Value="#FF9800"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    
                                    <!-- Colonne Produits avec style FNEV4 -->
                                    <DataGridTemplateColumn Header="Produits" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent" 
                                                        Padding="4"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center">
                                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                                            Command="{Binding Path=DataContext.ShowProductDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            ToolTip="Voir les détails des produits"
                                                            Height="36"
                                                            Padding="8,6"
                                                            FontSize="12"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            MinWidth="80">
                                                        <StackPanel Orientation="Horizontal">
                                                            <materialDesign:PackIcon Kind="Package" 
                                                                                   Width="14" Height="14" 
                                                                                   Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                                                                   VerticalAlignment="Center"
                                                                                   Margin="0,0,4,0" />
                                                            <TextBlock Text="{Binding NombreProduits}" 
                                                                       FontWeight="Medium" 
                                                                       VerticalAlignment="Center" />
                                                        </StackPanel>
                                                    </Button>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </materialDesign:Card>
                    </Grid>
                </materialDesign:Card>

                <!-- Section boutons d'action style Preview Window -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        
                        <!-- Barre de progression (visible seulement pendant l'import) -->
                        <materialDesign:Card Padding="16" 
                                           Margin="0,0,0,16"
                                           Background="#E3F2FD"
                                           Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisConverter}}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <materialDesign:PackIcon Kind="Import" 
                                                           Width="20" Height="20" 
                                                           Foreground="#1976D2"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0"/>
                                    <TextBlock Text="Import en cours..." 
                                             FontWeight="Medium"
                                             Foreground="#1976D2"
                                             VerticalAlignment="Center"/>
                                </StackPanel>
                                <ProgressBar IsIndeterminate="True"
                                           Height="8"
                                           Background="#BBDEFB"
                                           Foreground="#1976D2"
                                           materialDesign:TransitionAssist.DisableTransitions="False"/>
                            </StackPanel>
                        </materialDesign:Card>
                        
                        <!-- Boutons d'action -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            
                            <!-- Bouton Exporter -->
                            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Command="{Binding ExportToExcelCommand}"
                                    Margin="0,0,8,0"
                                    Padding="16,8"
                                    FontSize="14"
                                    MinWidth="120"
                                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileExcel" Width="18" Height="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="Exporter en Excel"/>
                                </StackPanel>
                            </Button>
                            
                            <!-- Bouton principal Import -->
                            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding ImportCommand}"
                                    Background="#FF5722"
                                    BorderBrush="#FF5722"
                                    Padding="16,8"
                                    FontSize="14"
                                    MinWidth="180"
                                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Import" Width="18" Height="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="IMPORTER LES FACTURES"/>
                                </StackPanel>
                            </Button>
                            
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Résultats import -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding HasImportResult, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Grid.Column="0"
                                                   Kind="{Binding ImportResultIcon}"
                                                   Width="32" Height="32"
                                                   Foreground="{Binding ImportResultColor}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,16,0"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding ImportResultMessage}" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="{Binding ImportResultDetails}" 
                                         Style="{StaticResource MaterialDesignBody2TextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Détails des factures importées -->
                        <Expander Header="Détails des factures importées" 
                                 Margin="0,16,0,0"
                                 Visibility="{Binding HasDetailedResults, Converter={StaticResource BoolToVisConverter}}">
                            <DataGrid ItemsSource="{Binding ImportedFactures}"
                                     Style="{StaticResource MaterialDesignDataGrid}"
                                     AutoGenerateColumns="False"
                                     IsReadOnly="True"
                                     Margin="0,8,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Feuille" Binding="{Binding NomFeuille}" Width="120"/>
                                    <DataGridTextColumn Header="N° Facture" Binding="{Binding NumeroFacture}" Width="100"/>
                                    <DataGridTextColumn Header="État" Binding="{Binding EstImportee, Converter={StaticResource BoolToStateConverter}}" Width="100"/>
                                    <DataGridTextColumn Header="Produits" Binding="{Binding NombreProduits}" Width="80"/>
                                    <DataGridTextColumn Header="Montant" Binding="{Binding MontantTotal, StringFormat='N0'}" Width="100"/>
                                    <DataGridTextColumn Header="Erreur" Binding="{Binding MessageErreur}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Expander>
                    </StackPanel>
                </materialDesign:Card>

            </StackPanel>
        </ScrollViewer>

        <!-- Barre de progression -->
        <ProgressBar Grid.Row="2" 
                    IsIndeterminate="{Binding IsProcessing}"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisConverter}}"
                    Style="{StaticResource MaterialDesignLinearProgressBar}"
                    Margin="0,16,0,0"/>
    </Grid>
</UserControl>
