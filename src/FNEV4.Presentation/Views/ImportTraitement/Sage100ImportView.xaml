<UserControl x:Class="FNEV4.Presentation.Views.ImportTraitement.Sage100ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:FNEV4.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
        <converters:BoolToIconConverter x:Key="BoolToIconConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToStateConverter x:Key="BoolToStateConverter"/>
        <converters:ProcessingTextConverter x:Key="ProcessingTextConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanAndConverter x:Key="BooleanAndConverter"/>
        <converters:ProduitsToTooltipConverter x:Key="ProduitsToTooltipConverter"/>
        
        <!-- Nouveaux convertisseurs pour améliorer la DataGrid -->
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>
        <converters:BoolToLightColorConverter x:Key="BoolToLightColorConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignHeadline5TextBlock}">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <Style x:Key="StepCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <materialDesign:PackIcon Grid.Column="0" 
                                   Kind="AlertCircleOutline" 
                                   Width="40" Height="40"
                                   VerticalAlignment="Center"
                                   Foreground="#FF5722"/>
            
            <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Import Exceptionnel Sage 100 v15" 
                         Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Import spécialisé pour factures Sage 100 v15 avec structure 1 feuille = 1 facture" 
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
            
            <Button Grid.Column="2" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding GoBackCommand}"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Retour"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Zone principale -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Section: Dossiers configurés et import automatique -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}" 
                                   Background="{DynamicResource MaterialDesignPaper}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Kind="FolderCog" 
                                                   Grid.Column="0"
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,12,0"
                                                   Width="24" Height="24"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Dossiers Configurés - Import Automatique" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="Traitement automatisé des fichiers depuis le dossier d'import configuré" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         Margin="0,2,0,0"/>
                            </StackPanel>
                            
                            <Button Grid.Column="2" 
                                   Content="Scanner" 
                                   Style="{StaticResource MaterialDesignOutlinedButton}"
                                   Command="{Binding ScanImportFolderCommand}"
                                   Margin="0,0,8,0"
                                   Width="100">
                                <Button.ToolTip>
                                    <ToolTip Content="Scanner le dossier d'import pour traiter tous les fichiers Excel"/>
                                </Button.ToolTip>
                            </Button>
                            
                            <Button Grid.Column="3" 
                                   Style="{StaticResource MaterialDesignIconButton}"
                                   Command="{Binding OpenConfiguredFoldersCommand}"
                                   Width="40" Height="40">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="20" Height="20"/>
                                <Button.ToolTip>
                                    <ToolTip Content="Ouvrir le dossier d'import dans l'explorateur"/>
                                </Button.ToolTip>
                            </Button>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Dossier Import -->
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="FolderDownload" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#4CAF50"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Import" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ImportFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ImportFolderPath}"/>
                            </StackPanel>
                            
                            <!-- Dossier Export -->
                            <StackPanel Grid.Column="1" Margin="0,0,16,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="FolderUpload" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#2196F3"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Export" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ExportFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ExportFolderPath}"/>
                            </StackPanel>
                            
                            <!-- Dossier Archive -->
                            <StackPanel Grid.Column="2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <materialDesign:PackIcon Kind="Archive" 
                                                           Grid.Column="0"
                                                           VerticalAlignment="Center" 
                                                           Margin="0,0,8,0"
                                                           Width="16" Height="16"
                                                           Foreground="#FF9800"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                             Text="Archive" 
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             FontWeight="Medium"/>
                                </Grid>
                                <TextBlock Text="{Binding ArchiveFolderPath}" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         TextTrimming="CharacterEllipsis"
                                         Margin="24,2,0,0"
                                         ToolTip="{Binding ArchiveFolderPath}"/>
                            </StackPanel>
                        </Grid>
                        
                        <CheckBox Content="Archiver automatiquement les fichiers après import" 
                                  IsChecked="{Binding AutoArchiveEnabled}"
                                  Margin="0,16,0,0"
                                  Style="{StaticResource MaterialDesignCheckBox}"/>
                    </StackPanel>
                </materialDesign:Card>

                <Separator Margin="0,8,0,16" Style="{StaticResource MaterialDesignSeparator}"/>
                
                <!-- Étape 1: Sélection du fichier -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" 
                               Background="#E3F2FD" 
                               CornerRadius="20" 
                               Width="40" Height="40"
                               Margin="0,0,16,0">
                            <TextBlock Text="1" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"
                                     FontWeight="Bold" 
                                     FontSize="16"
                                     Foreground="#1976D2"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Import Manuel - Sélection de fichier" 
                                     Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                     FontWeight="Medium"/>
                            <TextBlock Text="{Binding SelectedFilePath, TargetNullValue='Aucun fichier sélectionné - Alternative à l\'import automatique'}" 
                                     Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                                     Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <Button Grid.Column="2" 
                               Content="Parcourir"
                               Style="{StaticResource MaterialDesignRaisedButton}"
                               Command="{Binding SelectFileCommand}"
                               Width="120"/>
                    </Grid>
                </materialDesign:Card>

                <!-- Étape 2: Validation et aperçu -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding HasSelectedFile, Converter={StaticResource BoolToVisConverter}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- En-tête étape 2 -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                   Background="#E8F5E8" 
                                   CornerRadius="20" 
                                   Width="40" Height="40"
                                   Margin="0,0,16,0">
                                <TextBlock Text="2" 
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center"
                                         FontWeight="Bold" 
                                         FontSize="16"
                                         Foreground="#388E3C"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Validation et aperçu du fichier" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="Vérification de la structure Sage 100 v15 et prévisualisation des factures" 
                                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            </StackPanel>
                            
                            <Button Grid.Column="2" 
                                   Style="{StaticResource MaterialDesignRaisedButton}"
                                   Command="{Binding ValidateFileCommand}"
                                   Width="120">
                                <Button.IsEnabled>
                                    <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                                        <Binding Path="HasSelectedFile"/>
                                        <Binding Path="IsProcessing" Converter="{StaticResource InverseBooleanConverter}"/>
                                    </MultiBinding>
                                </Button.IsEnabled>
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="{Binding ValidationIcon, FallbackValue=CheckCircle}"
                                                               Width="16" Height="16"
                                                               Margin="0,0,8,0"
                                                               Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard RepeatBehavior="Forever">
                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle"
                                                                                       From="0" To="360" Duration="0:0:1"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <Setter Property="Kind" Value="Loading"/>
                                                            <Setter Property="RenderTransform">
                                                                <Setter.Value>
                                                                    <RotateTransform/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding IsProcessing, Converter={StaticResource ProcessingTextConverter}, FallbackValue=Valider}"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </Grid>
                        
                        <!-- Résultats validation -->
                        <Border Grid.Row="1" 
                               Background="{DynamicResource MaterialDesignDivider}"
                               CornerRadius="8"
                               Padding="16"
                               Margin="0,16,0,0"
                               Visibility="{Binding HasValidationResult, Converter={StaticResource BoolToVisConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:PackIcon Grid.Column="0"
                                                       Kind="{Binding ValidationIcon}"
                                                       Width="24" Height="24"
                                                       Foreground="{Binding ValidationColor}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,12,0"/>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding ValidationMessage}" 
                                             Style="{StaticResource MaterialDesignBody1TextBlock}"
                                             FontWeight="Medium"/>
                                    <TextBlock Text="{Binding ValidationDetails}" 
                                             Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                             Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- En-tête informatif pour la DataGrid -->
                        <Border Grid.Row="2" 
                               Background="#F8F9FA" 
                               CornerRadius="4" 
                               Padding="16,12"
                               Margin="0,16,0,8"
                               Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVisConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:PackIcon Grid.Column="0" 
                                                       Kind="InformationOutline"
                                                       VerticalAlignment="Center"
                                                       Foreground="#2196F3"
                                                       Width="20" Height="20"
                                                       Margin="0,0,12,0"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Prévisualisation des factures détectées"
                                             FontWeight="Medium"
                                             FontSize="14"
                                             Margin="0,0,0,4"/>
                                    <TextBlock Text="Vous pouvez redimensionner les colonnes et comparer les données avec votre fichier Excel. Les info-bulles affichent les données complètes."
                                             FontSize="12"
                                             Foreground="#666"
                                             TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <Border Grid.Column="2"
                                       Background="#E3F2FD"
                                       CornerRadius="12"
                                       Padding="8,4">
                                    <TextBlock Text="{Binding PreviewFactures.Count, StringFormat='{}{0} factures'}"
                                             FontWeight="Medium"
                                             FontSize="12"
                                             Foreground="#1976D2"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Section recherche et filtres -->
                        <Grid Grid.Row="3" 
                             Margin="0,8,0,8"
                             Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVisConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Recherche -->
                            <TextBox Grid.Column="0" 
                                    Style="{StaticResource MaterialDesignTextBox}"
                                    materialDesign:HintAssist.Hint="🔍 Rechercher (facture, client, montant...)"
                                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                    materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                    materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                                    Margin="0,0,16,0"/>
                            
                            <!-- Filtres -->
                            <ComboBox Grid.Column="1" 
                                     Style="{StaticResource MaterialDesignComboBox}"
                                     materialDesign:HintAssist.Hint="État"
                                     SelectedValue="{Binding FilterEtat}"
                                     SelectedValuePath="Tag"
                                     Width="120"
                                     Margin="0,0,8,0">
                                <ComboBoxItem Content="Tous" Tag="All"/>
                                <ComboBoxItem Content="✅ Valides" Tag="Valid"/>
                                <ComboBoxItem Content="❌ Erreurs" Tag="Error"/>
                            </ComboBox>
                            
                            <ComboBox Grid.Column="2" 
                                     Style="{StaticResource MaterialDesignComboBox}"
                                     materialDesign:HintAssist.Hint="Paiement"
                                     SelectedValue="{Binding FilterPaiement}"
                                     SelectedValuePath="Tag"
                                     Width="140">
                                <ComboBoxItem Content="Tous" Tag="All"/>
                                <ComboBoxItem Content="💰 Cash" Tag="cash"/>
                                <ComboBoxItem Content="🏦 Par défaut" Tag="default"/>
                                <ComboBoxItem Content="❗ Inexistant" Tag="missing"/>
                            </ComboBox>
                        </Grid>
                        
                        <!-- Aperçu des factures -->
                        <DataGrid Grid.Row="4" 
                                 ItemsSource="{Binding PreviewFacturesView}"
                                 Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVisConverter}}"
                                 Style="{StaticResource MaterialDesignDataGrid}"
                                 AutoGenerateColumns="False"
                                 IsReadOnly="True"
                                 Margin="0,8,0,0"
                                 CanUserResizeColumns="True"
                                 CanUserReorderColumns="True"
                                 GridLinesVisibility="Horizontal"
                                 HeadersVisibility="Column"
                                 AlternatingRowBackground="#FAFAFA"
                                 FontSize="12"
                                 RowHeight="32"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 VirtualizingPanel.IsContainerVirtualizable="True"
                                 ScrollViewer.CanContentScroll="True"
                                 EnableRowVirtualization="True"
                                 EnableColumnVirtualization="True"
                                 MaxHeight="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Feuille" Binding="{Binding NomFeuille}" MinWidth="80" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Medium"/>
                                            <Setter Property="Foreground" Value="#2196F3"/>
                                            <Setter Property="ToolTip" Value="{Binding NomFeuille}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTextColumn Header="N° Facture" Binding="{Binding NumeroFacture}" MinWidth="100" Width="110">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="#333"/>
                                            <Setter Property="ToolTip" Value="{Binding NumeroFacture}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTextColumn Header="Client" Binding="{Binding NomClient}" MinWidth="150" Width="180">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="NoWrap"/>
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding NomClient}"/>
                                            <Setter Property="Foreground" Value="#444"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTextColumn Header="Date" Binding="{Binding DateFacture, StringFormat='dd/MM/yyyy'}" MinWidth="80" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="ToolTip" Value="{Binding DateFacture, StringFormat='dddd dd MMMM yyyy'}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTextColumn Header="Paiement" Binding="{Binding MoyenPaiement}" MinWidth="120" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="NoWrap"/>
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding MoyenPaiement}"/>
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Foreground" Value="#666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTemplateColumn Header="📦 Articles" Width="90" MinWidth="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="#E3F2FD" CornerRadius="8" Padding="8,4">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <materialDesign:PackIcon Kind="Package" 
                                                                           Width="14" Height="14" 
                                                                           Foreground="#1976D2" 
                                                                           VerticalAlignment="Center" 
                                                                           Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding NombreProduits}" 
                                                             FontWeight="Medium" 
                                                             Foreground="#1976D2"
                                                             VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <Border.ToolTip>
                                                    <ToolTip MaxWidth="400" Background="#FAFAFA" BorderBrush="#E0E0E0">
                                                        <TextBlock Text="{Binding Produits, Converter={StaticResource ProduitsToTooltipConverter}}"
                                                                 FontFamily="Consolas"
                                                                 FontSize="11"
                                                                 Foreground="#333"
                                                                 TextWrapping="Wrap"/>
                                                    </ToolTip>
                                                </Border.ToolTip>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <DataGridTextColumn Header="Montant" Binding="{Binding MontantEstime, StringFormat='N0'}" MinWidth="100" Width="110">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                            <Setter Property="ToolTip" Value="{Binding MontantEstime, StringFormat='Montant: {0:N0} FCFA'}"/>
                                            <Setter Property="Margin" Value="0,0,8,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTextColumn Header="Type" Binding="{Binding TypeFacture}" MinWidth="60" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                            <Setter Property="Foreground" Value="#FF9800"/>
                                            <Setter Property="ToolTip" Value="Type de facturation FNE (B2B, B2C, B2G, B2F)"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <DataGridTemplateColumn Header="État" Width="70" MinWidth="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding EstValide, Converter={StaticResource BoolToLightColorConverter}}"
                                                   CornerRadius="10"
                                                   Padding="6,2"
                                                   HorizontalAlignment="Center">
                                                <materialDesign:PackIcon Kind="{Binding EstValide, Converter={StaticResource BoolToIconConverter}}"
                                                                       Foreground="{Binding EstValide, Converter={StaticResource BoolToColorConverter}}"
                                                                       Width="14" Height="14"/>
                                                <Border.ToolTip>
                                                    <TextBlock>
                                                        <Run Text="État: "/>
                                                        <Run Text="{Binding EstValide, Converter={StaticResource BoolToStatusConverter}}" FontWeight="Bold"/>
                                                        <LineBreak/>
                                                        <Run Text="Client trouvé: " FontWeight="Medium"/>
                                                        <Run Text="{Binding ClientTrouve, Converter={StaticResource BoolToYesNoConverter}}"/>
                                                    </TextBlock>
                                                </Border.ToolTip>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </materialDesign:Card>

                <!-- Étape 3: Import -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding CanImport, Converter={StaticResource BoolToVisConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" 
                               Background="#FFF3E0" 
                               CornerRadius="20" 
                               Width="40" Height="40"
                               Margin="0,0,16,0">
                            <TextBlock Text="3" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"
                                     FontWeight="Bold" 
                                     FontSize="16"
                                     Foreground="#F57C00"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Import des factures" 
                                     Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                     FontWeight="Medium"/>
                            <TextBlock Text="Traitement et intégration des factures en base de données" 
                                     Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        </StackPanel>
                        
                        <Button Grid.Column="2" 
                               Content="IMPORTER"
                               Style="{StaticResource MaterialDesignRaisedButton}"
                               Background="#FF5722"
                               BorderBrush="#FF5722"
                               Command="{Binding ImportCommand}"
                               IsEnabled="{Binding CanExecuteImport}"
                               Width="120"/>
                    </Grid>
                </materialDesign:Card>

                <!-- Résultats import -->
                <materialDesign:Card Style="{StaticResource StepCardStyle}"
                                   Visibility="{Binding HasImportResult, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Grid.Column="0"
                                                   Kind="{Binding ImportResultIcon}"
                                                   Width="32" Height="32"
                                                   Foreground="{Binding ImportResultColor}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,16,0"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding ImportResultMessage}" 
                                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                         FontWeight="Medium"/>
                                <TextBlock Text="{Binding ImportResultDetails}" 
                                         Style="{StaticResource MaterialDesignBody2TextBlock}"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Détails des factures importées -->
                        <Expander Header="Détails des factures importées" 
                                 Margin="0,16,0,0"
                                 Visibility="{Binding HasDetailedResults, Converter={StaticResource BoolToVisConverter}}">
                            <DataGrid ItemsSource="{Binding ImportedFactures}"
                                     Style="{StaticResource MaterialDesignDataGrid}"
                                     AutoGenerateColumns="False"
                                     IsReadOnly="True"
                                     Margin="0,8,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Feuille" Binding="{Binding NomFeuille}" Width="120"/>
                                    <DataGridTextColumn Header="N° Facture" Binding="{Binding NumeroFacture}" Width="100"/>
                                    <DataGridTextColumn Header="État" Binding="{Binding EstImportee, Converter={StaticResource BoolToStateConverter}}" Width="100"/>
                                    <DataGridTextColumn Header="Produits" Binding="{Binding NombreProduits}" Width="80"/>
                                    <DataGridTextColumn Header="Montant" Binding="{Binding MontantTotal, StringFormat='N0'}" Width="100"/>
                                    <DataGridTextColumn Header="Erreur" Binding="{Binding MessageErreur}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Expander>
                    </StackPanel>
                </materialDesign:Card>

            </StackPanel>
        </ScrollViewer>

        <!-- Barre de progression -->
        <ProgressBar Grid.Row="2" 
                    IsIndeterminate="{Binding IsProcessing}"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisConverter}}"
                    Style="{StaticResource MaterialDesignLinearProgressBar}"
                    Margin="0,16,0,0"/>
    </Grid>
</UserControl>
