<Window x:Class="FNEV4.Presentation.Views.ImportTraitement.Sage100FactureDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Détails de la Facture"
        Height="700"
        Width="1000"
        MinHeight="500"
        MinWidth="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="False"
        ResizeMode="CanResize">

    <Window.Resources>
        <!-- Style pour les en-têtes de section -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Margin" Value="0,12,0,8"/>
        </Style>

        <!-- Style pour les valeurs -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="8,2"/>
        </Style>

        <!-- Style pour les badges de statut -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête avec icône et titre -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <materialDesign:PackIcon Grid.Column="0"
                                     Kind="FileDocumentOutline"
                                     Width="32"
                                     Height="32"
                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,12,0"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding FactureTitle}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBlock Text="{Binding FactureSubtitle}"
                           FontSize="14"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>

            <!-- Badge de statut -->
            <Border Grid.Column="2"
                    Style="{StaticResource StatusBadgeStyle}"
                    Background="{Binding StatusBackground}">
                <TextBlock Text="{Binding StatusText}"
                           FontWeight="SemiBold"
                           Foreground="White"/>
            </Border>
        </Grid>

        <!-- Informations de la facture -->
        <materialDesign:Card Grid.Row="1" 
                             Margin="0,0,0,16"
                             Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Colonne 1: Informations client -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="📋 INFORMATIONS CLIENT" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Code:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.CodeClient}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nom:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Facture.NomClient}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Template:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <Border Grid.Row="2" Grid.Column="1" 
                                Background="{Binding TemplateBackground}"
                                CornerRadius="8"
                                Padding="6,2"
                                HorizontalAlignment="Left"
                                Margin="8,2">
                            <TextBlock Text="{Binding Facture.Template}" 
                                       FontWeight="SemiBold"
                                       Foreground="White"/>
                        </Border>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Point de Vente:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Facture.PointDeVente}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Colonne 2: Informations facture -->
                <StackPanel Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="📄 INFORMATIONS FACTURE" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="N° Facture:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.NumeroFacture}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Date:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Facture.DateFacture, StringFormat=dd/MM/yyyy}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Paiement:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Facture.MoyenPaiement}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Nb Produits:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Facture.NombreProduits}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Colonne 3: Montants -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="💰 MONTANTS" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Montant HT:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Facture.MontantHT, StringFormat='{}{0:N2} FCFA'}" Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Montant TTC:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Facture.MontantTTC, StringFormat='{}{0:N2} FCFA'}" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="TVA:" FontWeight="SemiBold" Style="{StaticResource ValueStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding MontantTVA, StringFormat='{}{0:N2} FCFA'}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- DataGrid des produits -->
        <materialDesign:Card Grid.Row="2" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- En-tête des produits -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Padding="16,12">
                    <TextBlock Text="🛍️ DÉTAIL DES PRODUITS"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Produits}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          AlternatingRowBackground="{DynamicResource MaterialDesignDivider}"
                          materialDesign:DataGridAssist.CellPadding="8,4"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8,8">

                    <DataGrid.Columns>
                        <!-- Code Produit -->
                        <DataGridTextColumn Header="Code Produit"
                                            Binding="{Binding CodeProduit}"
                                            Width="120"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Désignation -->
                        <DataGridTextColumn Header="Désignation"
                                            Binding="{Binding Designation}"
                                            Width="*"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Quantité -->
                        <DataGridTextColumn Header="Qté"
                                            Binding="{Binding Quantite, StringFormat=N2}"
                                            Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Emballage -->
                        <DataGridTextColumn Header="Emballage"
                                            Binding="{Binding Emballage}"
                                            Width="100"
                                            ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                        <!-- Prix Unitaire -->
                        <DataGridTextColumn Header="Prix Unit. (FCFA)"
                                            Binding="{Binding PrixUnitaire, StringFormat='{}{0:N2}'}"
                                            Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Type TVA selon documentation FNE -->
                        <DataGridTemplateColumn Header="Type TVA" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                        <!-- Code TVA -->
                                        <TextBlock HorizontalAlignment="Center"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Margin="0,2">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="">
                                                            <Setter Property="Text" Value="NON DÉFINI"/>
                                                            <Setter Property="Foreground" Value="#FF5722"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="NON DÉFINI"/>
                                                            <Setter Property="Foreground" Value="#FF5722"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                    <Setter Property="Text" Value="{Binding CodeTva}"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <!-- Description selon FNE -->
                                        <TextBlock FontSize="10" 
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,2">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="TVA">
                                                            <Setter Property="Text" Value="TVA normal de 18%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="TVAB">
                                                            <Setter Property="Text" Value="TVA réduit de 9%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="TVAC">
                                                            <Setter Property="Text" Value="TVA exec conv de 0%"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="TVAD">
                                                            <Setter Property="Text" Value="TVA exec leg de 0% pour TEE et RME"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="">
                                                            <Setter Property="Text" Value="Code TVA manquant"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CodeTva}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="Code TVA manquant"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                    <Setter Property="Text" Value="Type non reconnu"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Montant HT -->
                        <DataGridTextColumn Header="Montant HT (FCFA)"
                                            Binding="{Binding MontantHt, StringFormat='{}{0:N2}'}"
                                            Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Boutons d'action -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            
            <Button Content="📊 EXPORTER"
                    Command="{Binding ExporterCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,12,0"
                    Padding="16,8">
                <Button.ToolTip>
                    <ToolTip Content="Exporter les détails en Excel"/>
                </Button.ToolTip>
            </Button>

            <Button Content="❌ FERMER"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Padding="16,8"
                    Click="FermerButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
